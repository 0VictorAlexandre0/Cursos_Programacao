#'''Crie um programa onde o usuário possa digitar cinco valores# numéricos e cadastre-os em uma lista, já na posição correta de# inserção (sem usar o sort() ). No final, mostre a lista# ordenada na tela.'''lista = []for item in range(0, 5):    n = int(input('Digite um valor: '))    if item == 0: #se for o 1º valor, só colocar.        lista.append(n)        print('Adicionado ao final da lista...')    elif n > lista[len(lista)-1]: #se o número for maior do que o último elemento.        #ou lista[-1]        lista.append(n)    #ou if item == 0 or n > lista[-1]:            #lista.append(n)    else:        pos = 0        while pos < len(lista): #vai até a última posição da lista.            if n <= lista[pos]:            #verificar cada posição e colocar o N no lugar correto de acordo com os que ja existem la dentro.                lista.insert(pos, n) #inserir N na posição específica.                print(f'Adicionado na posição {pos} da lista...')                break            pos += 1print('-='*30)print(f'Os valores digitados em ordem foram {lista}')